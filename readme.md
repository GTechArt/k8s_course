# Kubernetes courses

## Launch services

1. Launch docker
2. Launch minikube : minikube start --extra-config="apiserver.cors-allowed-origins=['http://boot.dev']"
3. create dashboard : minikube dashboard --port=63840 -> (http://localhost:63840/)
